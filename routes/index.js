var express = require('express');
var router = express.Router();

var CLIENT_ROOT_FOLDER = 'client';

/* GET home page. */
// router.get('/', function(req, res, next) {
//   res.sendFile('index.html' ,{root: CLIENT_ROOT_FOLDER});
// });

module.exports = router;
